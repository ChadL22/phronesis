# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# newsletter automation — Google OAuth client + tokens, never commit
.google-credentials/

# old config
wrangler.jsonc

# cloudflare worker (deployed separately, not part of static site)
cloudflare-worker/

# local databases
databases/
